/**
 * Tick History
 * Tick History provides dynamic access to historical tick data for a specific security for specific dates or date range.
 *
 * The version of the OpenAPI document: 2.3.0
 * Contact: api@factset.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import CompletedResponse from '../model/CompletedResponse';
import CoverageResponse from '../model/CoverageResponse';
import GetFilesMinuteResponse from '../model/GetFilesMinuteResponse';
import GetFilesResponse from '../model/GetFilesResponse';
import LeveloneRequest from '../model/LeveloneRequest';
import MinRequest from '../model/MinRequest';
import Status from '../model/Status';
import SubmittedResponse from '../model/SubmittedResponse';

/**
* Level1 service.
* @module api/Level1Api
*/
export default class Level1Api {

    /**
    * Constructs a new Level1Api. 
    * @alias module:api/Level1Api
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }



    /**
     * Returns the status of the ID
     * Need to plug-in the id get from /create endpoint into /status endpoint
     * @param {String} id id returned by files create endpoint to poll and collect status of the query
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/CompletedResponse} and HTTP response
     */
    getLevel1FileStatusWithHttpInfo(id) {
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getLevel1FileStatus");
      }

      let pathParams = {
      };
      let queryParams = {
        'id': id
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];

      let returnType = GetLevel1FileStatusResponseWrapperTypeMap;
      
      return this.apiClient.callApi(
        '/level1/files/status', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Returns the status of the ID
     * Need to plug-in the id get from /create endpoint into /status endpoint
     * @param {String} id id returned by files create endpoint to poll and collect status of the query
     * @return { Promise.< GetLevel1FileStatusResponseWrapper > } a Promise, with data of type {@link GetLevel1FileStatusResponseWrapper }
     */
    getLevel1FileStatus(id) {
      return this.getLevel1FileStatusWithHttpInfo(id)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Returns the status of the ID
     * Need to plug-in the id get from /create endpoint into /status endpoint
     * @param {String} id id returned by files create endpoint to poll and collect status of the query
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/CompletedResponse} and HTTP response
     */
    getMinutebarsFileStatusWithHttpInfo(id) {
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getMinutebarsFileStatus");
      }

      let pathParams = {
      };
      let queryParams = {
        'id': id
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];

      let returnType = GetMinutebarsFileStatusResponseWrapperTypeMap;
      
      return this.apiClient.callApi(
        '/level1/files/minute-bars/status', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Returns the status of the ID
     * Need to plug-in the id get from /create endpoint into /status endpoint
     * @param {String} id id returned by files create endpoint to poll and collect status of the query
     * @return { Promise.< GetMinutebarsFileStatusResponseWrapper > } a Promise, with data of type {@link GetMinutebarsFileStatusResponseWrapper }
     */
    getMinutebarsFileStatus(id) {
      return this.getMinutebarsFileStatusWithHttpInfo(id)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Returns the Minute bars data in files requested in the /create endpoint
     * Returns the files from tickhistory endpoint
     * @param {String} id id returned by files create endpoint to poll and collect results of the query
     * @param {Object} opts Optional parameters
     * @param {Number} opts.paginationLimit Specifies the maximum number of results to return per response page (default to 20)
     * @param {Number} opts.paginationOffset Specifies the starting point for pagination. This parameter is used to identify the beginning of next set of results (default to 0)
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/GetFilesMinuteResponse} and HTTP response
     */
    getMinutebarsFilesWithHttpInfo(id, opts) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getMinutebarsFiles");
      }

      let pathParams = {
      };
      let queryParams = {
        'id': id,
        '_paginationLimit': opts['paginationLimit'],
        '_paginationOffset': opts['paginationOffset']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];

      let returnType = GetMinutebarsFilesResponseWrapperTypeMap;
      
      return this.apiClient.callApi(
        '/level1/files/minute-bars/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Returns the Minute bars data in files requested in the /create endpoint
     * Returns the files from tickhistory endpoint
     * @param {String} id id returned by files create endpoint to poll and collect results of the query
     * @param {Object} opts Optional parameters
     * @param {Number} opts.paginationLimit Specifies the maximum number of results to return per response page (default to 20)
     * @param {Number} opts.paginationOffset Specifies the starting point for pagination. This parameter is used to identify the beginning of next set of results (default to 0)
     * @return { Promise.< GetMinutebarsFilesResponseWrapper > } a Promise, with data of type {@link GetMinutebarsFilesResponseWrapper }
     */
    getMinutebarsFiles(id, opts) {
      return this.getMinutebarsFilesWithHttpInfo(id, opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Returns the tick-by-tick data in files requested in the /create endpoint
     * Returns the files from tickhistory endpoint
     * @param {String} id id returned by files create endpoint to poll and collect results of the query
     * @param {Object} opts Optional parameters
     * @param {Number} opts.paginationLimit Specifies the maximum number of results to return per response page (default to 20)
     * @param {Number} opts.paginationOffset Specifies the starting point for pagination. This parameter is used to identify the beginning of next set of results (default to 0)
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/GetFilesResponse} and HTTP response
     */
    getTickHistoryLevel1FilesWithHttpInfo(id, opts) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getTickHistoryLevel1Files");
      }

      let pathParams = {
      };
      let queryParams = {
        'id': id,
        '_paginationLimit': opts['paginationLimit'],
        '_paginationOffset': opts['paginationOffset']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];

      let returnType = GetTickHistoryLevel1FilesResponseWrapperTypeMap;
      
      return this.apiClient.callApi(
        '/level1/files/get', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Returns the tick-by-tick data in files requested in the /create endpoint
     * Returns the files from tickhistory endpoint
     * @param {String} id id returned by files create endpoint to poll and collect results of the query
     * @param {Object} opts Optional parameters
     * @param {Number} opts.paginationLimit Specifies the maximum number of results to return per response page (default to 20)
     * @param {Number} opts.paginationOffset Specifies the starting point for pagination. This parameter is used to identify the beginning of next set of results (default to 0)
     * @return { Promise.< GetTickHistoryLevel1FilesResponseWrapper > } a Promise, with data of type {@link GetTickHistoryLevel1FilesResponseWrapper }
     */
    getTickHistoryLevel1Files(id, opts) {
      return this.getTickHistoryLevel1FilesWithHttpInfo(id, opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Requests the creation of Minute bars file
     * 1-minute bars available from 20120101 to previous day. Per request able to fetch upto 6 months of data.
     * @param {module:model/MinRequest} minRequest 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/SubmittedResponse} and HTTP response
     */
    requestMinutebarsFilesWithHttpInfo(minRequest) {
      let postBody = minRequest;
      // verify the required parameter 'minRequest' is set
      if (minRequest === undefined || minRequest === null) {
        throw new Error("Missing the required parameter 'minRequest' when calling requestMinutebarsFiles");
      }

      let pathParams = {
      };
      let queryParams = {
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];


      let returnType = SubmittedResponse;

      return this.apiClient.callApi(
        '/level1/files/minute-bars/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Requests the creation of Minute bars file
     * 1-minute bars available from 20120101 to previous day. Per request able to fetch upto 6 months of data.
     * @param {module:model/MinRequest} minRequest 
     * @return { Promise.< module:model/SubmittedResponse > } a Promise, with data of type {@link module:model/SubmittedResponse }
     */
    requestMinutebarsFiles(minRequest) {
      return this.requestMinutebarsFilesWithHttpInfo(minRequest)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Returns the coverage for the requested ticker/isin along with other response fields for Level1 data.
     * Returns coverage for specified tickers for Level1 Data
     * @param {Object} opts Optional parameters
     * @param {Array.<String>} opts.tickers <P> The requested list of one or more tickers.</p>   The `tickers` and `isins`  parameters provide two different ways to specify the identifiers for data retrieved. Please enter either the tickers or isins parameter.   <P> Maximum 10 tickers per request</p>     
     * @param {String} opts.factsetExchangeCode <p>This parameter is used to filter the results based on the FactSet specific regional or composite exchange code.</p> **NOTE**:Does not support multiple factsetExchangeCodes
     * @param {Array.<String>} opts.isins <P> Specifies the ISIN of the record. </p>    Please enter either the `tickers` or `isins` parameter.     <P> Maximum 10 ISINs per request.</p>      
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/CoverageResponse} and HTTP response
     */
    requestTickHistoryCoverageWithHttpInfo(opts) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'tickers': this.apiClient.buildCollectionParam(opts['tickers'], 'csv'),
        'factsetExchangeCode': opts['factsetExchangeCode'],
        'isins': this.apiClient.buildCollectionParam(opts['isins'], 'csv')
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];


      let returnType = CoverageResponse;

      return this.apiClient.callApi(
        '/level1/coverage', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Returns the coverage for the requested ticker/isin along with other response fields for Level1 data.
     * Returns coverage for specified tickers for Level1 Data
     * @param {Object} opts Optional parameters
     * @param {Array.<String>} opts.tickers <P> The requested list of one or more tickers.</p>   The `tickers` and `isins`  parameters provide two different ways to specify the identifiers for data retrieved. Please enter either the tickers or isins parameter.   <P> Maximum 10 tickers per request</p>     
     * @param {String} opts.factsetExchangeCode <p>This parameter is used to filter the results based on the FactSet specific regional or composite exchange code.</p> **NOTE**:Does not support multiple factsetExchangeCodes
     * @param {Array.<String>} opts.isins <P> Specifies the ISIN of the record. </p>    Please enter either the `tickers` or `isins` parameter.     <P> Maximum 10 ISINs per request.</p>      
     * @return { Promise.< module:model/CoverageResponse > } a Promise, with data of type {@link module:model/CoverageResponse }
     */
    requestTickHistoryCoverage(opts) {
      return this.requestTickHistoryCoverageWithHttpInfo(opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Requests the creation of tick-by-tick file
     * Data available from 20120101 to previous day.
     * @param {module:model/LeveloneRequest} leveloneRequest 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/SubmittedResponse} and HTTP response
     */
    requestTickHistoryLevel1FilesWithHttpInfo(leveloneRequest) {
      let postBody = leveloneRequest;
      // verify the required parameter 'leveloneRequest' is set
      if (leveloneRequest === undefined || leveloneRequest === null) {
        throw new Error("Missing the required parameter 'leveloneRequest' when calling requestTickHistoryLevel1Files");
      }

      let pathParams = {
      };
      let queryParams = {
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];


      let returnType = SubmittedResponse;

      return this.apiClient.callApi(
        '/level1/files/create', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Requests the creation of tick-by-tick file
     * Data available from 20120101 to previous day.
     * @param {module:model/LeveloneRequest} leveloneRequest 
     * @return { Promise.< module:model/SubmittedResponse > } a Promise, with data of type {@link module:model/SubmittedResponse }
     */
    requestTickHistoryLevel1Files(leveloneRequest) {
      return this.requestTickHistoryLevel1FilesWithHttpInfo(leveloneRequest)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


}


const GetLevel1FileStatusResponseWrapperTypeMap = {
  201: CompletedResponse,
  202: SubmittedResponse,
  401: Status,
  403: Status,
  404: Status,
  500: Status,

  _createResponseWrapper(statusCode, response) {
    return new GetLevel1FileStatusResponseWrapper(statusCode, response);
  }
};

const GetMinutebarsFileStatusResponseWrapperTypeMap = {
  201: CompletedResponse,
  202: SubmittedResponse,
  401: Status,
  403: Status,
  404: Status,
  500: Status,

  _createResponseWrapper(statusCode, response) {
    return new GetMinutebarsFileStatusResponseWrapper(statusCode, response);
  }
};

const GetMinutebarsFilesResponseWrapperTypeMap = {
  200: GetFilesMinuteResponse,
  202: SubmittedResponse,
  400: Status,
  401: Status,
  403: Status,
  404: Status,
  500: Status,

  _createResponseWrapper(statusCode, response) {
    return new GetMinutebarsFilesResponseWrapper(statusCode, response);
  }
};

const GetTickHistoryLevel1FilesResponseWrapperTypeMap = {
  200: GetFilesResponse,
  202: SubmittedResponse,
  400: Status,
  401: Status,
  403: Status,
  404: Status,
  500: Status,

  _createResponseWrapper(statusCode, response) {
    return new GetTickHistoryLevel1FilesResponseWrapper(statusCode, response);
  }
};



/**
 * Wrapper to support GET /level1/files/status returning different types
 * per status code.
 *
 * <p>
 * Responses:
 * <ul>
 *   <li>201 : {@code CompletedResponse }<br> The Request has been created and the response has been created. </li>
 * 
 *   <li>202 : {@code SubmittedResponse }<br>The Request has not finished and the result has NOT been created. </li>
 * </ul>
 *
 * </p>
 * Example:
 * <pre>{@code
 * const response = ...;
 * switch (response.statusCode) {
 *   case 201:
 *     CompletedResponse data201 = response.getResponse201();
 *     break;
 *   case 202:
 *     SubmittedResponse data202 = response.getResponse202();
 *     break;
 *  }
 * }</pre>
 *
 * @alias module:GetLevel1FileStatusResponseWrapper
 * @class
 */
export class GetLevel1FileStatusResponseWrapper {

  /**
   * @param {number} statusCode
   * @param {*} response
   */
  constructor(statusCode, response) {
    /**
     * @type {number}
     */
    this.statusCode = statusCode;

    /**
     * @type {*}
     */
    this.response = response;
  }

  
  /**
   * @returns { CompletedResponse }
   */
  getResponse201() {
    if (this.statusCode !== 201) {
      throw new Error("Invalid response getter called. getResponse201 can't return a " + this.statusCode + " response");
    }

    return this.response;
  }
  
  /**
   * @returns { SubmittedResponse }
   */
  getResponse202() {
    if (this.statusCode !== 202) {
      throw new Error("Invalid response getter called. getResponse202 can't return a " + this.statusCode + " response");
    }

    return this.response;
  }
  
}

/**
 * Wrapper to support GET /level1/files/minute-bars/status returning different types
 * per status code.
 *
 * <p>
 * Responses:
 * <ul>
 *   <li>201 : {@code CompletedResponse }<br> The Request has been created and the response has been created. </li>
 * 
 *   <li>202 : {@code SubmittedResponse }<br>The Request has not finished and the result has NOT been created. </li>
 * </ul>
 *
 * </p>
 * Example:
 * <pre>{@code
 * const response = ...;
 * switch (response.statusCode) {
 *   case 201:
 *     CompletedResponse data201 = response.getResponse201();
 *     break;
 *   case 202:
 *     SubmittedResponse data202 = response.getResponse202();
 *     break;
 *  }
 * }</pre>
 *
 * @alias module:GetMinutebarsFileStatusResponseWrapper
 * @class
 */
export class GetMinutebarsFileStatusResponseWrapper {

  /**
   * @param {number} statusCode
   * @param {*} response
   */
  constructor(statusCode, response) {
    /**
     * @type {number}
     */
    this.statusCode = statusCode;

    /**
     * @type {*}
     */
    this.response = response;
  }

  
  /**
   * @returns { CompletedResponse }
   */
  getResponse201() {
    if (this.statusCode !== 201) {
      throw new Error("Invalid response getter called. getResponse201 can't return a " + this.statusCode + " response");
    }

    return this.response;
  }
  
  /**
   * @returns { SubmittedResponse }
   */
  getResponse202() {
    if (this.statusCode !== 202) {
      throw new Error("Invalid response getter called. getResponse202 can't return a " + this.statusCode + " response");
    }

    return this.response;
  }
  
}

/**
 * Wrapper to support GET /level1/files/minute-bars/get returning different types
 * per status code.
 *
 * <p>
 * Responses:
 * <ul>
 *   <li>200 : {@code GetFilesMinuteResponse }<br>Success&lt;/p&gt; **To download the output file copy the response url in a browser**</li>
 * 
 *   <li>202 : {@code SubmittedResponse }<br>The Request has not finished and the result has NOT been created. </li>
 * </ul>
 *
 * </p>
 * Example:
 * <pre>{@code
 * const response = ...;
 * switch (response.statusCode) {
 *   case 200:
 *     GetFilesMinuteResponse data200 = response.getResponse200();
 *     break;
 *   case 202:
 *     SubmittedResponse data202 = response.getResponse202();
 *     break;
 *  }
 * }</pre>
 *
 * @alias module:GetMinutebarsFilesResponseWrapper
 * @class
 */
export class GetMinutebarsFilesResponseWrapper {

  /**
   * @param {number} statusCode
   * @param {*} response
   */
  constructor(statusCode, response) {
    /**
     * @type {number}
     */
    this.statusCode = statusCode;

    /**
     * @type {*}
     */
    this.response = response;
  }

  
  /**
   * @returns { GetFilesMinuteResponse }
   */
  getResponse200() {
    if (this.statusCode !== 200) {
      throw new Error("Invalid response getter called. getResponse200 can't return a " + this.statusCode + " response");
    }

    return this.response;
  }
  
  /**
   * @returns { SubmittedResponse }
   */
  getResponse202() {
    if (this.statusCode !== 202) {
      throw new Error("Invalid response getter called. getResponse202 can't return a " + this.statusCode + " response");
    }

    return this.response;
  }
  
}

/**
 * Wrapper to support GET /level1/files/get returning different types
 * per status code.
 *
 * <p>
 * Responses:
 * <ul>
 *   <li>200 : {@code GetFilesResponse }<br>Success&lt;/p&gt; **To download the output file copy the response url in a browser**</li>
 * 
 *   <li>202 : {@code SubmittedResponse }<br>The Request has not finished and the result has NOT been created. </li>
 * </ul>
 *
 * </p>
 * Example:
 * <pre>{@code
 * const response = ...;
 * switch (response.statusCode) {
 *   case 200:
 *     GetFilesResponse data200 = response.getResponse200();
 *     break;
 *   case 202:
 *     SubmittedResponse data202 = response.getResponse202();
 *     break;
 *  }
 * }</pre>
 *
 * @alias module:GetTickHistoryLevel1FilesResponseWrapper
 * @class
 */
export class GetTickHistoryLevel1FilesResponseWrapper {

  /**
   * @param {number} statusCode
   * @param {*} response
   */
  constructor(statusCode, response) {
    /**
     * @type {number}
     */
    this.statusCode = statusCode;

    /**
     * @type {*}
     */
    this.response = response;
  }

  
  /**
   * @returns { GetFilesResponse }
   */
  getResponse200() {
    if (this.statusCode !== 200) {
      throw new Error("Invalid response getter called. getResponse200 can't return a " + this.statusCode + " response");
    }

    return this.response;
  }
  
  /**
   * @returns { SubmittedResponse }
   */
  getResponse202() {
    if (this.statusCode !== 202) {
      throw new Error("Invalid response getter called. getResponse202 can't return a " + this.statusCode + " response");
    }

    return this.response;
  }
  
}


