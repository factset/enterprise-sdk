/**
 * Natural Language Processing API
 * APIs that leverage Natural Language Processing to help extract meaningful data from unstructured text
 *
 * The version of the OpenAPI document: 1.3.0
 * Contact: api@factset.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NEROrganization from './NEROrganization';

/**
 * The NEREntity model module.
 * @module model/NEREntity
 */
class NEREntity {
    /**
     * Constructs a new <code>NEREntity</code>.
     * @alias module:model/NEREntity
     * @param endChar {Number} The index of the last character of the entity text
     * @param startChar {Number} The index of the starting character of the entity text
     * @param text {String} The substring of the source text that ws found as an entity
     * @param type {module:model/NEREntity.TypeEnum} The type of entity (organization, person, etc.) that was found
     */
    constructor(endChar, startChar, text, type) { 
        
        NEREntity.initialize(this, endChar, startChar, text, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, endChar, startChar, text, type) { 
        obj['endChar'] = endChar;
        obj['startChar'] = startChar;
        obj['text'] = text;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>NEREntity</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NEREntity} obj Optional instance to populate.
     * @return {module:model/NEREntity} The populated <code>NEREntity</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NEREntity();

            if (data.hasOwnProperty('associatedOrgs')) {
                obj['associatedOrgs'] = ApiClient.convertToType(data['associatedOrgs'], [NEROrganization]);
            }
            if (data.hasOwnProperty('endChar')) {
                obj['endChar'] = ApiClient.convertToType(data['endChar'], 'Number');
            }
            if (data.hasOwnProperty('entityId')) {
                obj['entityId'] = ApiClient.convertToType(data['entityId'], 'String');
            }
            if (data.hasOwnProperty('lookupText')) {
                obj['lookupText'] = ApiClient.convertToType(data['lookupText'], 'String');
            }
            if (data.hasOwnProperty('lookupUrl')) {
                obj['lookupUrl'] = ApiClient.convertToType(data['lookupUrl'], 'String');
            }
            if (data.hasOwnProperty('startChar')) {
                obj['startChar'] = ApiClient.convertToType(data['startChar'], 'Number');
            }
            if (data.hasOwnProperty('text')) {
                obj['text'] = ApiClient.convertToType(data['text'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }


}

/**
 * Organizations that are associated with this entity
 * @member {Array.<module:model/NEROrganization>} associatedOrgs
 */
NEREntity.prototype['associatedOrgs'] = undefined;

/**
 * The index of the last character of the entity text
 * @member {Number} endChar
 */
NEREntity.prototype['endChar'] = undefined;

/**
 * The FactSet Entity ID that is linked to this text.
 * @member {String} entityId
 */
NEREntity.prototype['entityId'] = undefined;

/**
 * Name of the FactSet entity that is linked to this text.
 * @member {String} lookupText
 */
NEREntity.prototype['lookupText'] = undefined;

/**
 * A link to the information page for the given entity
 * @member {String} lookupUrl
 */
NEREntity.prototype['lookupUrl'] = undefined;

/**
 * The index of the starting character of the entity text
 * @member {Number} startChar
 */
NEREntity.prototype['startChar'] = undefined;

/**
 * The substring of the source text that ws found as an entity
 * @member {String} text
 */
NEREntity.prototype['text'] = undefined;

/**
 * The type of entity (organization, person, etc.) that was found
 * @member {module:model/NEREntity.TypeEnum} type
 */
NEREntity.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
NEREntity['TypeEnum'] = {

    /**
     * value: "NE_GPE"
     * @const
     */
    "GPE": "NE_GPE",

    /**
     * value: "NE_ORG"
     * @const
     */
    "ORG": "NE_ORG",

    /**
     * value: "NE_PERSON"
     * @const
     */
    "PERSON": "NE_PERSON",

    /**
     * value: "NE_NUMBER"
     * @const
     */
    "NUMBER": "NE_NUMBER",

    /**
     * value: "NE_MONEY"
     * @const
     */
    "MONEY": "NE_MONEY",

    /**
     * value: "NE_DATETIME"
     * @const
     */
    "DATETIME": "NE_DATETIME",

    /**
     * value: "NE_DRUG"
     * @const
     */
    "DRUG": "NE_DRUG",

    /**
     * value: "NE_HEALTH_INDICATION"
     * @const
     */
    "HEALTH_INDICATION": "NE_HEALTH_INDICATION"
};



export default NEREntity;

