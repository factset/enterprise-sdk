/**
 * Cabot Models API
 * Cabot Models API
 *
 * The version of the OpenAPI document: 0.3.0
 * Contact: api@factset.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import Attributes from '../model/Attributes';
import ClientErrorResponse from '../model/ClientErrorResponse';
import ConstructionResponseRoot from '../model/ConstructionResponseRoot';
import Currency from '../model/Currency';
import HitRateResponseRoot from '../model/HitRateResponseRoot';
import Regions from '../model/Regions';
import ResultsResponseRoot from '../model/ResultsResponseRoot';
import Sectors from '../model/Sectors';
import SkillsHoldings from '../model/SkillsHoldings';
import SkillsResponseRoot from '../model/SkillsResponseRoot';

/**
* OverviewModels service.
* @module api/OverviewModelsApi
*/
export default class OverviewModelsApi {

    /**
    * Constructs a new OverviewModelsApi. 
    * @alias module:api/OverviewModelsApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }



    /**
     * Cabot main path for Construction API
     * Cabot main path for Construction API
     * @param {String} accountPath The account path of the portfolio you want to retrieve the data for.<br /><br />
     * @param {String} benchmarkPath The path of the benchmark you want to retrieve the data for.<br /><br />
     * @param {String} period For which period you want to retrieve the data.<br />There are four options available as follows:<br /><br />1 -> YYYY (Repeating One Year)<br /><br />2 -> YYYY-YYYY (Repeating Three/Five/Ten Year)<br /><br />3 -> 1M_TRAILING, 3M_TRAILING, 1Y_TRAILING, 3Y_TRAILING, 5Y_TRAILING (Trailing Periods (If available for your portfolio))<br /><br />4 -> INCEPTION_TO_DATE<br /><br />You can only get the data for one period per request.<br /><br />
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/ConstructionResponseRoot} and HTTP response
     */
    getConstructionModelAnalyticWithHttpInfo(accountPath, benchmarkPath, period) {
      let postBody = null;
      // verify the required parameter 'accountPath' is set
      if (accountPath === undefined || accountPath === null) {
        throw new Error("Missing the required parameter 'accountPath' when calling getConstructionModelAnalytic");
      }
      // verify the required parameter 'benchmarkPath' is set
      if (benchmarkPath === undefined || benchmarkPath === null) {
        throw new Error("Missing the required parameter 'benchmarkPath' when calling getConstructionModelAnalytic");
      }
      // verify the required parameter 'period' is set
      if (period === undefined || period === null) {
        throw new Error("Missing the required parameter 'period' when calling getConstructionModelAnalytic");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountPath': accountPath,
        'benchmarkPath': benchmarkPath,
        'period': period
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];


      let returnType = ConstructionResponseRoot;

      return this.apiClient.callApi(
        '/models/construction', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Cabot main path for Construction API
     * Cabot main path for Construction API
     * @param {String} accountPath The account path of the portfolio you want to retrieve the data for.<br /><br />
     * @param {String} benchmarkPath The path of the benchmark you want to retrieve the data for.<br /><br />
     * @param {String} period For which period you want to retrieve the data.<br />There are four options available as follows:<br /><br />1 -> YYYY (Repeating One Year)<br /><br />2 -> YYYY-YYYY (Repeating Three/Five/Ten Year)<br /><br />3 -> 1M_TRAILING, 3M_TRAILING, 1Y_TRAILING, 3Y_TRAILING, 5Y_TRAILING (Trailing Periods (If available for your portfolio))<br /><br />4 -> INCEPTION_TO_DATE<br /><br />You can only get the data for one period per request.<br /><br />
     * @return { Promise.< module:model/ConstructionResponseRoot > } a Promise, with data of type {@link module:model/ConstructionResponseRoot }
     */
    getConstructionModelAnalytic(accountPath, benchmarkPath, period) {
      return this.getConstructionModelAnalyticWithHttpInfo(accountPath, benchmarkPath, period)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Cabot main path for Hit Rate API
     * Cabot main path for Hit Rate API
     * @param {String} accountPath The account path of the portfolio you want to retrieve the data for.<br /><br />
     * @param {String} benchmarkPath The path of the benchmark you want to retrieve the data for.<br /><br />
     * @param {String} period For which period you want to retrieve the data.<br />There are four options available as follows:<br /><br />1 -> YYYY (Repeating One Year)<br /><br />2 -> YYYY-YYYY (Repeating Three/Five/Ten Year)<br /><br />3 -> 1M_TRAILING, 3M_TRAILING, 1Y_TRAILING, 3Y_TRAILING, 5Y_TRAILING (Trailing Periods (If available for your portfolio))<br /><br />4 -> INCEPTION_TO_DATE<br /><br />You can only get the data for one period per request.<br /><br />
     * @param {module:model/Sectors} sector Sector represents the sector based on the company's industry breakdown.<br />You can choose which of them (if any) you want to see analytics for.<br /><br />
     * @param {module:model/Regions} region Region of domicile represents the region based on the company's primary listing.<br />You can choose which of them (if any) you want to see analytics for.<br /><br />
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/HitRateResponseRoot} and HTTP response
     */
    getHitRateModelAnalyticWithHttpInfo(accountPath, benchmarkPath, period, sector, region) {
      let postBody = null;
      // verify the required parameter 'accountPath' is set
      if (accountPath === undefined || accountPath === null) {
        throw new Error("Missing the required parameter 'accountPath' when calling getHitRateModelAnalytic");
      }
      // verify the required parameter 'benchmarkPath' is set
      if (benchmarkPath === undefined || benchmarkPath === null) {
        throw new Error("Missing the required parameter 'benchmarkPath' when calling getHitRateModelAnalytic");
      }
      // verify the required parameter 'period' is set
      if (period === undefined || period === null) {
        throw new Error("Missing the required parameter 'period' when calling getHitRateModelAnalytic");
      }
      // verify the required parameter 'sector' is set
      if (sector === undefined || sector === null) {
        throw new Error("Missing the required parameter 'sector' when calling getHitRateModelAnalytic");
      }
      // verify the required parameter 'region' is set
      if (region === undefined || region === null) {
        throw new Error("Missing the required parameter 'region' when calling getHitRateModelAnalytic");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountPath': accountPath,
        'benchmarkPath': benchmarkPath,
        'period': period,
        'sector': sector,
        'region': region
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];


      let returnType = HitRateResponseRoot;

      return this.apiClient.callApi(
        '/models/hit-rate', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Cabot main path for Hit Rate API
     * Cabot main path for Hit Rate API
     * @param {String} accountPath The account path of the portfolio you want to retrieve the data for.<br /><br />
     * @param {String} benchmarkPath The path of the benchmark you want to retrieve the data for.<br /><br />
     * @param {String} period For which period you want to retrieve the data.<br />There are four options available as follows:<br /><br />1 -> YYYY (Repeating One Year)<br /><br />2 -> YYYY-YYYY (Repeating Three/Five/Ten Year)<br /><br />3 -> 1M_TRAILING, 3M_TRAILING, 1Y_TRAILING, 3Y_TRAILING, 5Y_TRAILING (Trailing Periods (If available for your portfolio))<br /><br />4 -> INCEPTION_TO_DATE<br /><br />You can only get the data for one period per request.<br /><br />
     * @param {module:model/Sectors} sector Sector represents the sector based on the company's industry breakdown.<br />You can choose which of them (if any) you want to see analytics for.<br /><br />
     * @param {module:model/Regions} region Region of domicile represents the region based on the company's primary listing.<br />You can choose which of them (if any) you want to see analytics for.<br /><br />
     * @return { Promise.< module:model/HitRateResponseRoot > } a Promise, with data of type {@link module:model/HitRateResponseRoot }
     */
    getHitRateModelAnalytic(accountPath, benchmarkPath, period, sector, region) {
      return this.getHitRateModelAnalyticWithHttpInfo(accountPath, benchmarkPath, period, sector, region)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Cabot main path for Results API
     * Cabot main path for Results API.     Describes portfolio returns in comparison to its benchmark. A positive value indicates excess returns while a negative value indicates underperformance.
     * @param {String} accountPath The account path of the portfolio you want to retrieve the data for.<br /><br />
     * @param {String} benchmarkPath The path of the benchmark you want to retrieve the data for.<br /><br />
     * @param {String} period For which period you want to retrieve the data.<br />There are four options available as follows:<br /><br />1 -> YYYY (Repeating One Year)<br /><br />2 -> YYYY-YYYY (Repeating Three/Five/Ten Year)<br /><br />3 -> 1M_TRAILING, 3M_TRAILING, 1Y_TRAILING, 3Y_TRAILING, 5Y_TRAILING (Trailing Periods (If available for your portfolio))<br /><br />4 -> INCEPTION_TO_DATE<br /><br />You can only get the data for one period per request.<br /><br />
     * @param {Object} opts Optional parameters
     * @param {module:model/Currency} opts.currency In which currency you want to see the data. By default the API uses USD.<br /><br />
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/ResultsResponseRoot} and HTTP response
     */
    getResultsModelAnalyticWithHttpInfo(accountPath, benchmarkPath, period, opts) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountPath' is set
      if (accountPath === undefined || accountPath === null) {
        throw new Error("Missing the required parameter 'accountPath' when calling getResultsModelAnalytic");
      }
      // verify the required parameter 'benchmarkPath' is set
      if (benchmarkPath === undefined || benchmarkPath === null) {
        throw new Error("Missing the required parameter 'benchmarkPath' when calling getResultsModelAnalytic");
      }
      // verify the required parameter 'period' is set
      if (period === undefined || period === null) {
        throw new Error("Missing the required parameter 'period' when calling getResultsModelAnalytic");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountPath': accountPath,
        'benchmarkPath': benchmarkPath,
        'period': period,
        'currency': opts['currency']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];


      let returnType = ResultsResponseRoot;

      return this.apiClient.callApi(
        '/models/results', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Cabot main path for Results API
     * Cabot main path for Results API.     Describes portfolio returns in comparison to its benchmark. A positive value indicates excess returns while a negative value indicates underperformance.
     * @param {String} accountPath The account path of the portfolio you want to retrieve the data for.<br /><br />
     * @param {String} benchmarkPath The path of the benchmark you want to retrieve the data for.<br /><br />
     * @param {String} period For which period you want to retrieve the data.<br />There are four options available as follows:<br /><br />1 -> YYYY (Repeating One Year)<br /><br />2 -> YYYY-YYYY (Repeating Three/Five/Ten Year)<br /><br />3 -> 1M_TRAILING, 3M_TRAILING, 1Y_TRAILING, 3Y_TRAILING, 5Y_TRAILING (Trailing Periods (If available for your portfolio))<br /><br />4 -> INCEPTION_TO_DATE<br /><br />You can only get the data for one period per request.<br /><br />
     * @param {Object} opts Optional parameters
     * @param {module:model/Currency} opts.currency In which currency you want to see the data. By default the API uses USD.<br /><br />
     * @return { Promise.< module:model/ResultsResponseRoot > } a Promise, with data of type {@link module:model/ResultsResponseRoot }
     */
    getResultsModelAnalytic(accountPath, benchmarkPath, period, opts) {
      return this.getResultsModelAnalyticWithHttpInfo(accountPath, benchmarkPath, period, opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Cabot main path for Skills API
     * Cabot main path for Skills API
     * @param {String} accountPath The account path of the portfolio you want to retrieve the data for.
     * @param {String} benchmarkPath The path of the benchmark you want to retrieve the data for.<br /><br />
     * @param {String} period For which period you want to retrieve the data.<br />There are four options available as follows:<br /><br />1 -> YYYY (Repeating One Year)<br /><br />2 -> YYYY-YYYY (Repeating Three/Five/Ten Year)<br /><br />3 -> 1M_TRAILING, 3M_TRAILING, 1Y_TRAILING, 3Y_TRAILING, 5Y_TRAILING (Trailing Periods (If available for your portfolio))<br /><br />4 -> INCEPTION_TO_DATE<br /><br />You can only get the data for one period per request.<br /><br />
     * @param {Object} opts Optional parameters
     * @param {module:model/Attributes} opts.attribute The attribute represents the different factors.<br />You can choose which of them (if any) you want to see analytics for.<br /><br />If provided, the API response will contain both \"LOW\" and \"HIGH\" values for it.<br /><br />
     * @param {module:model/Sectors} opts.sector Sector represents the sector based on the company's industry breakdown.<br />You can choose which of them (if any) you want to see analytics for.<br /><br />
     * @param {module:model/Regions} opts.region Region of domicile represents the region based on the company's primary listing.<br />You can choose which of them (if any) you want to see analytics for.<br /><br />
     * @param {module:model/SkillsHoldings} opts.holdings If set, the API will add holding-level details for all or only the selected analytic (if available for your portfolio).<br /><br />
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/SkillsResponseRoot} and HTTP response
     */
    getSkillsModelAnalyticWithHttpInfo(accountPath, benchmarkPath, period, opts) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'accountPath' is set
      if (accountPath === undefined || accountPath === null) {
        throw new Error("Missing the required parameter 'accountPath' when calling getSkillsModelAnalytic");
      }
      // verify the required parameter 'benchmarkPath' is set
      if (benchmarkPath === undefined || benchmarkPath === null) {
        throw new Error("Missing the required parameter 'benchmarkPath' when calling getSkillsModelAnalytic");
      }
      // verify the required parameter 'period' is set
      if (period === undefined || period === null) {
        throw new Error("Missing the required parameter 'period' when calling getSkillsModelAnalytic");
      }

      let pathParams = {
      };
      let queryParams = {
        'accountPath': accountPath,
        'benchmarkPath': benchmarkPath,
        'period': period,
        'attribute': opts['attribute'],
        'sector': opts['sector'],
        'region': opts['region'],
        'holdings': opts['holdings']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];


      let returnType = SkillsResponseRoot;

      return this.apiClient.callApi(
        '/models/skills', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Cabot main path for Skills API
     * Cabot main path for Skills API
     * @param {String} accountPath The account path of the portfolio you want to retrieve the data for.
     * @param {String} benchmarkPath The path of the benchmark you want to retrieve the data for.<br /><br />
     * @param {String} period For which period you want to retrieve the data.<br />There are four options available as follows:<br /><br />1 -> YYYY (Repeating One Year)<br /><br />2 -> YYYY-YYYY (Repeating Three/Five/Ten Year)<br /><br />3 -> 1M_TRAILING, 3M_TRAILING, 1Y_TRAILING, 3Y_TRAILING, 5Y_TRAILING (Trailing Periods (If available for your portfolio))<br /><br />4 -> INCEPTION_TO_DATE<br /><br />You can only get the data for one period per request.<br /><br />
     * @param {Object} opts Optional parameters
     * @param {module:model/Attributes} opts.attribute The attribute represents the different factors.<br />You can choose which of them (if any) you want to see analytics for.<br /><br />If provided, the API response will contain both \"LOW\" and \"HIGH\" values for it.<br /><br />
     * @param {module:model/Sectors} opts.sector Sector represents the sector based on the company's industry breakdown.<br />You can choose which of them (if any) you want to see analytics for.<br /><br />
     * @param {module:model/Regions} opts.region Region of domicile represents the region based on the company's primary listing.<br />You can choose which of them (if any) you want to see analytics for.<br /><br />
     * @param {module:model/SkillsHoldings} opts.holdings If set, the API will add holding-level details for all or only the selected analytic (if available for your portfolio).<br /><br />
     * @return { Promise.< module:model/SkillsResponseRoot > } a Promise, with data of type {@link module:model/SkillsResponseRoot }
     */
    getSkillsModelAnalytic(accountPath, benchmarkPath, period, opts) {
      return this.getSkillsModelAnalyticWithHttpInfo(accountPath, benchmarkPath, period, opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


}





