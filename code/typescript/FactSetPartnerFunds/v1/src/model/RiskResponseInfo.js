/**
 * FactSet Partner Funds API
 * Partner Funds API provides profile, allocations, performance and ranking information for mutual funds. The data is sourced from different vendors.  Currently the available partner is Fundata, a leading provider of Canadian mutual fund data. 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: api@factset.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RiskResponseInfo model module.
 * @module model/RiskResponseInfo
 */
class RiskResponseInfo {
    /**
     * Constructs a new <code>RiskResponseInfo</code>.
     * Metrics for a particular year
     * @alias module:model/RiskResponseInfo
     */
    constructor() { 
        
        RiskResponseInfo.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RiskResponseInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RiskResponseInfo} obj Optional instance to populate.
     * @return {module:model/RiskResponseInfo} The populated <code>RiskResponseInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RiskResponseInfo();

            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
            if (data.hasOwnProperty('benchmark')) {
                obj['benchmark'] = ApiClient.convertToType(data['benchmark'], 'String');
            }
            if (data.hasOwnProperty('alpha')) {
                obj['alpha'] = ApiClient.convertToType(data['alpha'], 'Number');
            }
            if (data.hasOwnProperty('beta')) {
                obj['beta'] = ApiClient.convertToType(data['beta'], 'Number');
            }
            if (data.hasOwnProperty('sharpeRatio')) {
                obj['sharpeRatio'] = ApiClient.convertToType(data['sharpeRatio'], 'Number');
            }
            if (data.hasOwnProperty('treynorRatio')) {
                obj['treynorRatio'] = ApiClient.convertToType(data['treynorRatio'], 'Number');
            }
            if (data.hasOwnProperty('rSquared')) {
                obj['rSquared'] = ApiClient.convertToType(data['rSquared'], 'Number');
            }
            if (data.hasOwnProperty('sortino')) {
                obj['sortino'] = ApiClient.convertToType(data['sortino'], 'Number');
            }
            if (data.hasOwnProperty('standardDeviation')) {
                obj['standardDeviation'] = ApiClient.convertToType(data['standardDeviation'], 'Number');
            }
            if (data.hasOwnProperty('volatility')) {
                obj['volatility'] = ApiClient.convertToType(data['volatility'], 'Number');
            }
        }
        return obj;
    }


}

/**
 * Year
 * @member {Number} year
 */
RiskResponseInfo.prototype['year'] = undefined;

/**
 * Benchmark
 * @member {String} benchmark
 */
RiskResponseInfo.prototype['benchmark'] = undefined;

/**
 * A measure of the difference between a fund's actual monthly excess return and its expected monthly excess return, which in turn is based on that fund's sensitivity (beta) to the excess return for the benchmark index.
 * @member {Number} alpha
 */
RiskResponseInfo.prototype['alpha'] = undefined;

/**
 * A measure of a fund's sensitivity to the market movement. The benchmark portfolio has the beta of one. A fund with beta greater than one is riskier than the benchmark and a fund with beta smaller than one is less risky than the benchmark.
 * @member {Number} beta
 */
RiskResponseInfo.prototype['beta'] = undefined;

/**
 * A measure of risk adjusted performance with the assumption of a well diversified portfolio, which only contains market risk. It is the ratio of a fund's excess return to its beta or market risk.
 * @member {Number} sharpeRatio
 */
RiskResponseInfo.prototype['sharpeRatio'] = undefined;

/**
 * A measure of risk adjusted performance. It is the ratio of a fund's excess return to its standard deviation. A higher Sharpe ratio normally preferred. This indicates a higher return for the amount of risk demonstrated by the fund.
 * @member {Number} treynorRatio
 */
RiskResponseInfo.prototype['treynorRatio'] = undefined;

/**
 * A statistic to measure the percentage of a fund's movements that are explained by the movements of its benchmark. A R-square with 100 means the portfolio performance is 100 percent explained by its benchmark. A high R-square also means the higher reliability of beta.
 * @member {Number} rSquared
 */
RiskResponseInfo.prototype['rSquared'] = undefined;

/**
 * The Sortino Ratio is similar to the Sharpe Ratio, except instead of using standard deviation as the denominator, it uses downside deviation.
 * @member {Number} sortino
 */
RiskResponseInfo.prototype['sortino'] = undefined;

/**
 * Standard deviation is a measure of risk which takes a fund monthly return over a certain period, e.g. 36 months and it calculates the average deviation from the mean. The higher the standard deviation, the riskier the fund is.
 * @member {Number} standardDeviation
 */
RiskResponseInfo.prototype['standardDeviation'] = undefined;

/**
 * Volatility is a measure of risk using a ranking of Standard Deviation in percentiles among its peer group. A scale from 1 to 10 is used to indicate in which decile the fund falls. A fund with 10 volatility is the highest and 1 is the lowest.
 * @member {Number} volatility
 */
RiskResponseInfo.prototype['volatility'] = undefined;






export default RiskResponseInfo;

