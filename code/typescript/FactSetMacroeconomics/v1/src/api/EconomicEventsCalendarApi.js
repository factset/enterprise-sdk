/**
 * FactSet Macroeconomics API
 * The FactSet Macroeconomics API provides access to economic calendar events and historical time series data for global economic indicators. Use the reference endpoints to discover available indicators, regions, and FDS codes before querying event or series data. 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: api@factset.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import ErrorResponse from '../model/ErrorResponse';
import EventCountResponse from '../model/EventCountResponse';
import EventResponse from '../model/EventResponse';

/**
* EconomicEventsCalendar service.
* @module api/EconomicEventsCalendarApi
*/
export default class EconomicEventsCalendarApi {

    /**
    * Constructs a new EconomicEventsCalendarApi. 
    * @alias module:api/EconomicEventsCalendarApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }



    /**
     * Retrieve economic calendar events
     * Returns economic calendar events for a specified date range, with optional filtering by country, indicator, and FDS code. Use `/meta/events` to discover available filter values (indicators, countries, FDS codes).
     * @param {Object} opts Optional parameters
     * @param {String} opts.fdsCode FDS code to filter events by a specific economic indicator. Use the `/meta/events?type=fds-codes` endpoint to retrieve all available codes.  - **Required:** startDate and endDate parameters must be provided when using this filter 
     * @param {String} opts.indicator Economic indicator name for filtering events. Use the `/meta/events?type=indicators` endpoint to retrieve available indicators.  - **Required:** startDate and endDate parameters must be provided when using this filter 
     * @param {String} opts.countryCode ISO country code for filtering events by country. Use the `/meta/events?type=countries` endpoint to retrieve available country codes. 
     * @param {String} opts.timeZone Time zone for event dates and times in GMT offset format (e.g., GMT-5, GMT, GMT+9). Use the `/meta/events?type=TIMEZONES` endpoint to retrieve available time zone values. If left blank, the default time zone is GMT.  (default to 'GMT')
     * @param {Date} opts.startDate Start date for the query range.  - **Required:** when `fdsCode` or `indicator` is specified - **Default:** If both `startDate` and `endDate` are omitted, the API defaults to the most recent available calendar day - **Maximum date range limit:** The maximum date range limit between `startDate` and `endDate` is up to 90 days. Requests with date ranges exceeding 90 days will result in a validation error. 
     * @param {Date} opts.endDate End date for the query range.  - **Required:** when `fdsCode` or `indicator` is specified - **Default:** If both `startDate` and `endDate` are omitted, the API defaults to the most recent available calendar day - **Maximum date range limit:** The maximum date range limit between `startDate` and `endDate` is up to 90 days. Requests with date ranges exceeding 90 days will result in a validation error. 
     * @param {Array.<module:model/String>} opts.fields Comma-separated list of fields to include in the `items` array for each event. Common event fields (`requestedFdsCode`, `eventName`, `countryCode`, `indicator`) are always returned. When omitted, all available fields in the items array are returned.  **Available fields (for items array):** | Field | Description | |---|---| | `eventDateTime` | Scheduled release date and time (UTC) | | `period` | Reference period covered by the data | | `actual` | Released actual value | | `prior` | Value from the prior period | | `consensus` | Consensus market estimate | | `surprise` | Difference between actual and consensus | 
     * @param {Number} opts.paginationLimit Maximum number of results to return per page. If not specified, the default page size is applied.  (default to 25)
     * @param {Number} opts.paginationOffset Number of results to skip before returning the first result. Used for pagination in combination with `_paginationLimit`.  (default to 0)
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/EventResponse} and HTTP response
     */
    getEconomicEventsWithHttpInfo(opts) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'fdsCode': opts['fdsCode'],
        'indicator': opts['indicator'],
        'countryCode': opts['countryCode'],
        'timeZone': opts['timeZone'],
        'startDate': opts['startDate'],
        'endDate': opts['endDate'],
        'fields': this.apiClient.buildCollectionParam(opts['fields'], 'csv'),
        '_paginationLimit': opts['paginationLimit'],
        '_paginationOffset': opts['paginationOffset']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];


      let returnType = EventResponse;

      return this.apiClient.callApi(
        '/economic-events', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Retrieve economic calendar events
     * Returns economic calendar events for a specified date range, with optional filtering by country, indicator, and FDS code. Use `/meta/events` to discover available filter values (indicators, countries, FDS codes).
     * @param {Object} opts Optional parameters
     * @param {String} opts.fdsCode FDS code to filter events by a specific economic indicator. Use the `/meta/events?type=fds-codes` endpoint to retrieve all available codes.  - **Required:** startDate and endDate parameters must be provided when using this filter 
     * @param {String} opts.indicator Economic indicator name for filtering events. Use the `/meta/events?type=indicators` endpoint to retrieve available indicators.  - **Required:** startDate and endDate parameters must be provided when using this filter 
     * @param {String} opts.countryCode ISO country code for filtering events by country. Use the `/meta/events?type=countries` endpoint to retrieve available country codes. 
     * @param {String} opts.timeZone Time zone for event dates and times in GMT offset format (e.g., GMT-5, GMT, GMT+9). Use the `/meta/events?type=TIMEZONES` endpoint to retrieve available time zone values. If left blank, the default time zone is GMT.  (default to 'GMT')
     * @param {Date} opts.startDate Start date for the query range.  - **Required:** when `fdsCode` or `indicator` is specified - **Default:** If both `startDate` and `endDate` are omitted, the API defaults to the most recent available calendar day - **Maximum date range limit:** The maximum date range limit between `startDate` and `endDate` is up to 90 days. Requests with date ranges exceeding 90 days will result in a validation error. 
     * @param {Date} opts.endDate End date for the query range.  - **Required:** when `fdsCode` or `indicator` is specified - **Default:** If both `startDate` and `endDate` are omitted, the API defaults to the most recent available calendar day - **Maximum date range limit:** The maximum date range limit between `startDate` and `endDate` is up to 90 days. Requests with date ranges exceeding 90 days will result in a validation error. 
     * @param {Array.<module:model/String>} opts.fields Comma-separated list of fields to include in the `items` array for each event. Common event fields (`requestedFdsCode`, `eventName`, `countryCode`, `indicator`) are always returned. When omitted, all available fields in the items array are returned.  **Available fields (for items array):** | Field | Description | |---|---| | `eventDateTime` | Scheduled release date and time (UTC) | | `period` | Reference period covered by the data | | `actual` | Released actual value | | `prior` | Value from the prior period | | `consensus` | Consensus market estimate | | `surprise` | Difference between actual and consensus | 
     * @param {Number} opts.paginationLimit Maximum number of results to return per page. If not specified, the default page size is applied.  (default to 25)
     * @param {Number} opts.paginationOffset Number of results to skip before returning the first result. Used for pagination in combination with `_paginationLimit`.  (default to 0)
     * @return { Promise.< module:model/EventResponse > } a Promise, with data of type {@link module:model/EventResponse }
     */
    getEconomicEvents(opts) {
      return this.getEconomicEventsWithHttpInfo(opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Retrieve economic events count
     * Returns the number of economic events per calendar day for a specified date range. Weekends can be included or excluded via the `includeWeekends` parameter.  Note: The response includes only dates with one or more events. Dates with no events are omitted from the response. 
     * @param {Object} opts Optional parameters
     * @param {Date} opts.startDate Start date for the query range.  - **Default:** If both `startDate` and `endDate` are omitted, the API defaults to the most recent available calendar day - **Maximum date range limit:** The maximum date range limit between `startDate` and `endDate` is up to 90 days. Requests with date ranges exceeding 90 days will result in a validation error. 
     * @param {Date} opts.endDate End date for the query range.  - **Default:** If both `startDate` and `endDate` are omitted, the API defaults to the most recent available calendar day - **Maximum date range limit:** The maximum date range limit between `startDate` and `endDate` is up to 90 days. Requests with date ranges exceeding 90 days will result in a validation error. 
     * @param {Boolean} opts.includeWeekends Returned daily counts include weekends by default. Set this to `false` to exclude weekends. (default to true)
     * @param {Number} opts.paginationLimit Maximum number of results to return per page. If not specified, the default page size is applied.  (default to 25)
     * @param {Number} opts.paginationOffset Number of results to skip before returning the first result. Used for pagination in combination with `_paginationLimit`.  (default to 0)
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/EventCountResponse} and HTTP response
     */
    getEconomicEventsCountWithHttpInfo(opts) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'startDate': opts['startDate'],
        'endDate': opts['endDate'],
        'includeWeekends': opts['includeWeekends'],
        '_paginationLimit': opts['paginationLimit'],
        '_paginationOffset': opts['paginationOffset']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];


      let returnType = EventCountResponse;

      return this.apiClient.callApi(
        '/event-count', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Retrieve economic events count
     * Returns the number of economic events per calendar day for a specified date range. Weekends can be included or excluded via the `includeWeekends` parameter.  Note: The response includes only dates with one or more events. Dates with no events are omitted from the response. 
     * @param {Object} opts Optional parameters
     * @param {Date} opts.startDate Start date for the query range.  - **Default:** If both `startDate` and `endDate` are omitted, the API defaults to the most recent available calendar day - **Maximum date range limit:** The maximum date range limit between `startDate` and `endDate` is up to 90 days. Requests with date ranges exceeding 90 days will result in a validation error. 
     * @param {Date} opts.endDate End date for the query range.  - **Default:** If both `startDate` and `endDate` are omitted, the API defaults to the most recent available calendar day - **Maximum date range limit:** The maximum date range limit between `startDate` and `endDate` is up to 90 days. Requests with date ranges exceeding 90 days will result in a validation error. 
     * @param {Boolean} opts.includeWeekends Returned daily counts include weekends by default. Set this to `false` to exclude weekends. (default to true)
     * @param {Number} opts.paginationLimit Maximum number of results to return per page. If not specified, the default page size is applied.  (default to 25)
     * @param {Number} opts.paginationOffset Number of results to skip before returning the first result. Used for pagination in combination with `_paginationLimit`.  (default to 0)
     * @return { Promise.< module:model/EventCountResponse > } a Promise, with data of type {@link module:model/EventCountResponse }
     */
    getEconomicEventsCount(opts) {
      return this.getEconomicEventsCountWithHttpInfo(opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


}





