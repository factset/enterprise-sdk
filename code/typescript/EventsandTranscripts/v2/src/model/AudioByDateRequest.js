/**
 * Events and Transcripts API
 * <p>The Calendar Events API provides access to FactSetâ€™s Event Calendar data alongside business logic that allows users to replicate views and functionality consistent with the experience provided by the Workstation. This API provides the ability to pull Event Calendar data based on specific filters.</p> <p>Events Audio API provides access to historical as well as the latest audio recordings of various company events covered by FactSet. The events include, but are not limited to: earnings calls, conferences, and investor days. This API also provides relevant metadata such as timestamps and identifiers around each audio file.</p> <p>The  Near Real-time Transcripts API enables access to Near Real-time Transcripts provided by CallStreet to time-sensitive clients. This API also provides the relevant speaker metadata along with their confidence scores. This data caters to quant clients interested in building machine learning models. Clients can leverage this API to perform sentiment analysis through natural language processing or machine learning. It can also be used to complement analysis using FactSet's transcripts service.</p> <p>Transcripts API provides conference call transcripts for companies' publicly held conference calls and a wealth of information regarding upcoming corporate events, such as conference call date and time, phone number and password, type of conference call, and important company investor relations contact information.</p> 
 *
 * The version of the OpenAPI document: 2.2.0
 * Contact: api@factset.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AudioByDateRequest model module.
 * @module model/AudioByDateRequest
 */
class AudioByDateRequest {
    /**
     * Constructs a new <code>AudioByDateRequest</code>.
     * Search criteria for audio files based on date range.
     * @alias module:model/AudioByDateRequest
     */
    constructor() { 
        
        AudioByDateRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AudioByDateRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AudioByDateRequest} obj Optional instance to populate.
     * @return {module:model/AudioByDateRequest} The populated <code>AudioByDateRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AudioByDateRequest();

            if (data.hasOwnProperty('startDate')) {
                obj['startDate'] = ApiClient.convertToType(data['startDate'], 'Date');
            }
            if (data.hasOwnProperty('endDate')) {
                obj['endDate'] = ApiClient.convertToType(data['endDate'], 'Date');
            }
            if (data.hasOwnProperty('startDateRelative')) {
                obj['startDateRelative'] = ApiClient.convertToType(data['startDateRelative'], 'Number');
            }
            if (data.hasOwnProperty('endDateRelative')) {
                obj['endDateRelative'] = ApiClient.convertToType(data['endDateRelative'], 'Number');
            }
            if (data.hasOwnProperty('audioSource')) {
                obj['audioSource'] = ApiClient.convertToType(data['audioSource'], 'String');
            }
            if (data.hasOwnProperty('ids')) {
                obj['ids'] = ApiClient.convertToType(data['ids'], ['String']);
            }
            if (data.hasOwnProperty('trimmed')) {
                obj['trimmed'] = ApiClient.convertToType(data['trimmed'], 'Boolean');
            }
        }
        return obj;
    }


}

/**
 * The earliest date of the audio file the API should fetch for. - Format: Should be absolute (YYYY-MM-DD). - *Either `startDate` or `startDateRelative` should be used, but not both.* - Data available from 10th May 2011 onwards. - If dates are not provided, default will return all files. 
 * @member {Date} startDate
 */
AudioByDateRequest.prototype['startDate'] = undefined;

/**
 * The latest date of the audio file the API should fetch for. - Format: Should be absolute (YYYY-MM-DD). - *Either `endDate` or `endDateRelative` should be used, but not both.* - If dates are not provided, default will return all files. 
 * @member {Date} endDate
 */
AudioByDateRequest.prototype['endDate'] = undefined;

/**
 * The earliest date of the feed file the API should fetch based on the file timestamp, relative to today. - Format: Integer ('0' for today, '-1' for yesterday, etc.). - *Either `startDate` or `startDateRelative` should be used, but not both.* - If dates are not provided, default will return all files. 
 * @member {Number} startDateRelative
 */
AudioByDateRequest.prototype['startDateRelative'] = undefined;

/**
 * The latest date of the feed file the API should fetch based on the file timestamp, relative to today. - Format: Integer ('0' for today, '-1' for yesterday, etc.). - *Either `endDate` or `endDateRelative` should be used, but not both.* - If dates are not provided, default will return all files. 
 * @member {Number} endDateRelative
 */
AudioByDateRequest.prototype['endDateRelative'] = undefined;

/**
 * Source of the Audio file: * Phone = Originated from phone call * Webcast = Originated from a webcast * Vendor = Received from vendor * WebcastReplay = Replay of a webcast * Flash = Identical to webcast; can merge with \"Webcast\" in the future * Replay = Phone replay          
 * @member {module:model/AudioByDateRequest.AudioSourceEnum} audioSource
 */
AudioByDateRequest.prototype['audioSource'] = undefined;

/**
 * Filter results based on ticker-region or Entity ID.
 * @member {Array.<String>} ids
 */
AudioByDateRequest.prototype['ids'] = undefined;

/**
 * Search for trimmed audio files (non-speaking portions removed).
 * @member {Boolean} trimmed
 * @default false
 */
AudioByDateRequest.prototype['trimmed'] = false;





/**
 * Allowed values for the <code>audioSource</code> property.
 * @enum {String}
 * @readonly
 */
AudioByDateRequest['AudioSourceEnum'] = {

    /**
     * value: "Phone"
     * @const
     */
    "Phone": "Phone",

    /**
     * value: "Webcast"
     * @const
     */
    "Webcast": "Webcast",

    /**
     * value: "Vendor"
     * @const
     */
    "Vendor": "Vendor",

    /**
     * value: "WebcastReplay"
     * @const
     */
    "WebcastReplay": "WebcastReplay",

    /**
     * value: "Flash"
     * @const
     */
    "Flash": "Flash",

    /**
     * value: "Replay"
     * @const
     */
    "Replay": "Replay"
};



export default AudioByDateRequest;

