/**
 * Events and Transcripts API
 * <p>The Calendar Events API provides access to FactSetâ€™s Event Calendar data alongside business logic that allows users to replicate views and functionality consistent with the experience provided by the Workstation. This API provides the ability to pull Event Calendar data based on specific filters.</p> <p>Events Audio API provides access to historical as well as the latest audio recordings of various company events covered by FactSet. The events include, but are not limited to: earnings calls, conferences, and investor days. This API also provides relevant metadata such as timestamps and identifiers around each audio file.</p> <p>The  Near Real-time Transcripts API enables access to Near Real-time Transcripts provided by CallStreet to time-sensitive clients. This API also provides the relevant speaker metadata along with their confidence scores. This data caters to quant clients interested in building machine learning models. Clients can leverage this API to perform sentiment analysis through natural language processing or machine learning. It can also be used to complement analysis using FactSet's transcripts service.</p> <p>Transcripts API provides conference call transcripts for companies' publicly held conference calls and a wealth of information regarding upcoming corporate events, such as conference call date and time, phone number and password, type of conference call, and important company investor relations contact information.</p> 
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: api@factset.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TranscriptsByIdsRequest model module.
 * @module model/TranscriptsByIdsRequest
 */
class TranscriptsByIdsRequest {
    /**
     * Constructs a new <code>TranscriptsByIdsRequest</code>.
     * Search criteria for transcripts based on various IDs and categories.
     * @alias module:model/TranscriptsByIdsRequest
     * @param ids {Array.<String>} Requested symbols or securities. This is a list with a maximum limit of 1000. Each symbol can be a FactSet exchange symbol, CUSIP, SEDOL, ISIN, or Entity ID
     */
    constructor(ids) { 
        
        TranscriptsByIdsRequest.initialize(this, ids);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, ids) { 
        obj['ids'] = ids;
    }

    /**
     * Constructs a <code>TranscriptsByIdsRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TranscriptsByIdsRequest} obj Optional instance to populate.
     * @return {module:model/TranscriptsByIdsRequest} The populated <code>TranscriptsByIdsRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TranscriptsByIdsRequest();

            if (data.hasOwnProperty('primaryId')) {
                obj['primaryId'] = ApiClient.convertToType(data['primaryId'], 'Boolean');
            }
            if (data.hasOwnProperty('ids')) {
                obj['ids'] = ApiClient.convertToType(data['ids'], ['String']);
            }
            if (data.hasOwnProperty('reportIds')) {
                obj['reportIds'] = ApiClient.convertToType(data['reportIds'], ['String']);
            }
            if (data.hasOwnProperty('categories')) {
                obj['categories'] = ApiClient.convertToType(data['categories'], ['String']);
            }
            if (data.hasOwnProperty('startDate')) {
                obj['startDate'] = ApiClient.convertToType(data['startDate'], 'Date');
            }
            if (data.hasOwnProperty('endDate')) {
                obj['endDate'] = ApiClient.convertToType(data['endDate'], 'Date');
            }
            if (data.hasOwnProperty('searchText')) {
                obj['searchText'] = ApiClient.convertToType(data['searchText'], 'String');
            }
        }
        return obj;
    }


}

/**
 * If true, search only for documents where the provided `ids` are the primary identifiers. If false, search for any mention.  **Note:** Transcripts only have a primary ID, so the results remain the same regardless of whether primaryId=true or primaryId=false is specified in the request. 
 * @member {Boolean} primaryId
 * @default false
 */
TranscriptsByIdsRequest.prototype['primaryId'] = false;

/**
 * Requested symbols or securities. This is a list with a maximum limit of 1000. Each symbol can be a FactSet exchange symbol, CUSIP, SEDOL, ISIN, or Entity ID
 * @member {Array.<String>} ids
 */
TranscriptsByIdsRequest.prototype['ids'] = undefined;

/**
 * List of report IDs (max 1000).
 * @member {Array.<String>} reportIds
 */
TranscriptsByIdsRequest.prototype['reportIds'] = undefined;

/**
 * List of category codes (country, industry, subject). Use `/meta/categories` endpoint. Default is all.
 * @member {Array.<String>} categories
 */
TranscriptsByIdsRequest.prototype['categories'] = undefined;

/**
 * Start Date (YYYY-MM-DD). 
 * @member {Date} startDate
 */
TranscriptsByIdsRequest.prototype['startDate'] = undefined;

/**
 * End Date (YYYY-MM-DD). 
 * @member {Date} endDate
 */
TranscriptsByIdsRequest.prototype['endDate'] = undefined;

/**
 * Text to search for within the transcript document stories.
 * @member {String} searchText
 */
TranscriptsByIdsRequest.prototype['searchText'] = undefined;






export default TranscriptsByIdsRequest;

