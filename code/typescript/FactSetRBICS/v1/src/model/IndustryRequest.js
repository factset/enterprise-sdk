/**
 * FactSet RBICS
 * FactSet Revere Business Industry Classification System (RBICS) is a comprehensive structured taxonomy to classify companies by what they primarily do. It delivers a granular view for investors by classifying companies using a bottom-up approach according to the products and services they provide. By combining this approach with a top-level grouping based on companies' behavior similarities and stock co-movement, FactSet RBICS delivers unprecedented precision for 1,400+ sector groups.  RBICS Focus is a six level classification which highlights the companys main business. It is based on the company business line/s that stand for majority of a the revenue that the company generates. Updated monthly. For more details, visit [Online Assistant Page #20710](https://oa.apps.factset.com/pages/20710).<p><b>Rate limit is set to 10 requests per second</b>.</p> 
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: api@factset.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The IndustryRequest model module.
 * @module model/IndustryRequest
 */
class IndustryRequest {
    /**
     * Constructs a new <code>IndustryRequest</code>.
     * Industry Request Body
     * @alias module:model/IndustryRequest
     * @param rbicsIds {Array.<String>} A list of RBICS industry classification identifiers. These are used to filter the results to specific industries. IDs can range from Level 1 (Economy) to Level 6 (Sub-Industry). A complete list of valid RBICS IDs can be retrieved using the `/structure` endpoints.
     */
    constructor(rbicsIds) { 
        
        IndustryRequest.initialize(this, rbicsIds);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, rbicsIds) { 
        obj['rbicsIds'] = rbicsIds;
    }

    /**
     * Constructs a <code>IndustryRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IndustryRequest} obj Optional instance to populate.
     * @return {module:model/IndustryRequest} The populated <code>IndustryRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IndustryRequest();

            if (data.hasOwnProperty('rbicsIds') && obj['rbicsIds'] === undefined) {
                obj['rbicsIds'] = ApiClient.convertToType(data['rbicsIds'], ['String']);
            }
            if (data.hasOwnProperty('startDate') && obj['startDate'] === undefined) {
                obj['startDate'] = ApiClient.convertToType(data['startDate'], 'Date');
            }
            if (data.hasOwnProperty('endDate') && obj['endDate'] === undefined) {
                obj['endDate'] = ApiClient.convertToType(data['endDate'], 'Date');
            }
            
        }
        return obj;
    }


}

/**
 * A list of RBICS industry classification identifiers. These are used to filter the results to specific industries. IDs can range from Level 1 (Economy) to Level 6 (Sub-Industry). A complete list of valid RBICS IDs can be retrieved using the `/structure` endpoints.
 * @member {Array.<String>} rbicsIds
 */
IndustryRequest.prototype['rbicsIds'] = undefined;

/**
 * Specifies the start date for the requested date range, formatted as **YYYY-MM-DD**. The data returned will be reflective of the classifications or revenue data as of this date and forward. The `startDate` must be equal to or before the `endDate`. Future dates (T+1) are not accepted in this endpoint. <br> Note: <br> - If omitted while `endDate` is specified: Data will be fetched from the earliest available record up to the specified end date. <br> - If both `startDate` and `endDate` are omitted: The response will return the latest available data.
 * @member {Date} startDate
 */
IndustryRequest.prototype['startDate'] = undefined;

/**
 * Specifies the end date for the requested date range, formatted as **YYYY-MM-DD**. The data returned will be reflective of the classifications or revenue data as of this date and earlier. The `endDate` must be equal to or after the `startDate`. Future dates (T+1) are not accepted in this endpoint. <br> Note: <br> - If omitted (with `startDate` specified): Data will be returned from the specified start date up to the most recent available date. <br> - If both `startDate` and `endDate` are omitted: The response will return the latest available data.
 * @member {Date} endDate
 */
IndustryRequest.prototype['endDate'] = undefined;






export default IndustryRequest;

