/**
 * FactSet RBICS
 * FactSet Revere Business Industry Classification System (RBICS) is a comprehensive structured taxonomy to classify companies by what they primarily do. It delivers a granular view for investors by classifying companies using a bottom-up approach according to the products and services they provide. By combining this approach with a top-level grouping based on companies' behavior similarities and stock co-movement, FactSet RBICS delivers unprecedented precision for 1,400+ sector groups.  RBICS Focus is a six level classification which highlights the companys main business. It is based on the company business line/s that stand for majority of a the revenue that the company generates. Updated monthly. For more details, visit [Online Assistant Page #20710](https://oa.apps.factset.com/pages/20710).<p><b>Rate limit is set to 10 requests per second</b>.</p> 
 *
 * The version of the OpenAPI document: 1.4.0
 * Contact: api@factset.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import ErrorsResponse from '../model/ErrorsResponse';
import IndustryFocusResponse from '../model/IndustryFocusResponse';
import IndustryRequest from '../model/IndustryRequest';
import IndustryRevenueRequest from '../model/IndustryRevenueRequest';
import IndustryRevenueResponse from '../model/IndustryRevenueResponse';

/**
* Industry service.
* @module api/IndustryApi
*/
export default class IndustryApi {

    /**
    * Constructs a new IndustryApi. 
    * @alias module:api/IndustryApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }



    /**
     * Get the list of companies with a specific RBICS Focus classification.
     * Identify all companies whose primary business falls within a specific RBICS industry. This endpoint screens your universe based on the RBICS Focus classification.   A company's Focus is the lowest-level (Level 6) RBICS sub-industry from which it derives 50% or more of its annual revenue.   Provide a list of one or more RBICS IDs (up to Level 6) to retrieve the list of companies whose primary business line falls within those sectors. A complete list of valid RBICS IDs can be retrieved using the `/structure` endpoints. Use the `startDate` and `endDate` parameters to screen for companies that held this classification during a specific historical period. 
     * @param {Array.<String>} rbicsIds A list of RBICS industry classification identifiers. These are used to filter the results to specific industries. IDs can range from Level 1 (Economy) to Level 6 (Sub-Industry). A complete list of valid RBICS IDs can be retrieved using the `/structure` endpoints. 
     * @param {Object} opts Optional parameters
     * @param {Date} opts.startDate Specifies the start date for the requested date range, formatted as **YYYY-MM-DD**. The data returned will be reflective of the classifications or revenue data as of this date and forward. The `startDate` must be equal to or before the `endDate`. Future dates (T+1) are not accepted in this endpoint.  Note:  - If omitted while `endDate` is specified: Data will be fetched from the earliest available record up to the specified end date. - If both `startDate` and `endDate` are omitted: The response will return the latest available data. 
     * @param {Date} opts.endDate Specifies the end date for the requested date range, formatted as **YYYY-MM-DD**. The data returned will be reflective of the classifications or revenue data as of this date and earlier. The `endDate` must be equal to or after the `startDate`. Future dates (T+1) are not accepted in this endpoint.  Note: - If omitted (with `startDate` specified): Data will be returned from the specified start date up to the most recent available date. - If both `startDate` and `endDate` are omitted: The response will return the latest available data. 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/IndustryFocusResponse} and HTTP response
     */
    getRbicsIndustryFocusWithHttpInfo(rbicsIds, opts) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'rbicsIds' is set
      if (rbicsIds === undefined || rbicsIds === null) {
        throw new Error("Missing the required parameter 'rbicsIds' when calling getRbicsIndustryFocus");
      }

      let pathParams = {
      };
      let queryParams = {
        'rbicsIds': this.apiClient.buildCollectionParam(rbicsIds, 'csv'),
        'startDate': opts['startDate'],
        'endDate': opts['endDate']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];


      let returnType = IndustryFocusResponse;

      return this.apiClient.callApi(
        '/factset-rbics/v1/industry/focus', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Get the list of companies with a specific RBICS Focus classification.
     * Identify all companies whose primary business falls within a specific RBICS industry. This endpoint screens your universe based on the RBICS Focus classification.   A company's Focus is the lowest-level (Level 6) RBICS sub-industry from which it derives 50% or more of its annual revenue.   Provide a list of one or more RBICS IDs (up to Level 6) to retrieve the list of companies whose primary business line falls within those sectors. A complete list of valid RBICS IDs can be retrieved using the `/structure` endpoints. Use the `startDate` and `endDate` parameters to screen for companies that held this classification during a specific historical period. 
     * @param {Array.<String>} rbicsIds A list of RBICS industry classification identifiers. These are used to filter the results to specific industries. IDs can range from Level 1 (Economy) to Level 6 (Sub-Industry). A complete list of valid RBICS IDs can be retrieved using the `/structure` endpoints. 
     * @param {Object} opts Optional parameters
     * @param {Date} opts.startDate Specifies the start date for the requested date range, formatted as **YYYY-MM-DD**. The data returned will be reflective of the classifications or revenue data as of this date and forward. The `startDate` must be equal to or before the `endDate`. Future dates (T+1) are not accepted in this endpoint.  Note:  - If omitted while `endDate` is specified: Data will be fetched from the earliest available record up to the specified end date. - If both `startDate` and `endDate` are omitted: The response will return the latest available data. 
     * @param {Date} opts.endDate Specifies the end date for the requested date range, formatted as **YYYY-MM-DD**. The data returned will be reflective of the classifications or revenue data as of this date and earlier. The `endDate` must be equal to or after the `startDate`. Future dates (T+1) are not accepted in this endpoint.  Note: - If omitted (with `startDate` specified): Data will be returned from the specified start date up to the most recent available date. - If both `startDate` and `endDate` are omitted: The response will return the latest available data. 
     * @return { Promise.< module:model/IndustryFocusResponse > } a Promise, with data of type {@link module:model/IndustryFocusResponse }
     */
    getRbicsIndustryFocus(rbicsIds, opts) {
      return this.getRbicsIndustryFocusWithHttpInfo(rbicsIds, opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Get the list of companies with a specific Industry Focus classification.
     * Identify all companies whose primary business falls within a specific RBICS industry. This endpoint screens your universe based on the RBICS Focus classification.  A company's Focus is the lowest-level (Level 6) RBICS sub-industry from which it derives 50% or more of its annual revenue.  Provide a list of one or more RBICS IDs (up to Level 6) to retrieve the list of companies whose primary business line falls within those sectors. Use the `startDate` and `endDate` parameters to screen for companies that held this classification during a specific historical period. 
     * @param {module:model/IndustryRequest} industryRequest Request Body to request a list of Industry Focus objects.
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/IndustryFocusResponse} and HTTP response
     */
    getRbicsIndustryFocusForListWithHttpInfo(industryRequest) {
      let postBody = industryRequest;
      // verify the required parameter 'industryRequest' is set
      if (industryRequest === undefined || industryRequest === null) {
        throw new Error("Missing the required parameter 'industryRequest' when calling getRbicsIndustryFocusForList");
      }

      let pathParams = {
      };
      let queryParams = {
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];


      let returnType = IndustryFocusResponse;

      return this.apiClient.callApi(
        '/factset-rbics/v1/industry/focus', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Get the list of companies with a specific Industry Focus classification.
     * Identify all companies whose primary business falls within a specific RBICS industry. This endpoint screens your universe based on the RBICS Focus classification.  A company's Focus is the lowest-level (Level 6) RBICS sub-industry from which it derives 50% or more of its annual revenue.  Provide a list of one or more RBICS IDs (up to Level 6) to retrieve the list of companies whose primary business line falls within those sectors. Use the `startDate` and `endDate` parameters to screen for companies that held this classification during a specific historical period. 
     * @param {module:model/IndustryRequest} industryRequest Request Body to request a list of Industry Focus objects.
     * @return { Promise.< module:model/IndustryFocusResponse > } a Promise, with data of type {@link module:model/IndustryFocusResponse }
     */
    getRbicsIndustryFocusForList(industryRequest) {
      return this.getRbicsIndustryFocusForListWithHttpInfo(industryRequest)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Get companies and their revenue exposure to a specific RBICS L6 Sub-Industry.
     * Screen for companies with revenue exposure to a specific RBICS Level 6 Sub-Industry. This endpoint identifies all companies that derive revenue from a specified sub-industry and returns the percentage of their total revenue attributable to that industry. This is a powerful tool for understanding the competitive landscape and supply chains at a granular level, beyond a company's primary classification. **Note:** The `rbicsIds` parameter for this endpoint must be a valid Level 6 Sub-Industry ID. Use the `startDate` and `endDate` parameters to control the time period for the underlying revenue reports. 
     * @param {Array.<String>} rbicsIds List of RBICS Level 6 Sub-Industry IDs to screen for companies by revenue exposure. **Note:** Only Level 6 IDs are supported for this endpoint. A complete list of valid Level 6 IDs can be retrieved using the `/structure` endpoints. 
     * @param {Object} opts Optional parameters
     * @param {Date} opts.startDate Specifies the start date for the requested date range, formatted as **YYYY-MM-DD**. The data returned will be reflective of the classifications or revenue data as of this date and forward. The `startDate` must be equal to or before the `endDate`. Future dates (T+1) are not accepted in this endpoint.  Note:  - If omitted while `endDate` is specified: Data will be fetched from the earliest available record up to the specified end date. - If both `startDate` and `endDate` are omitted: The response will return the latest available data. 
     * @param {Date} opts.endDate Specifies the end date for the requested date range, formatted as **YYYY-MM-DD**. The data returned will be reflective of the classifications or revenue data as of this date and earlier. The `endDate` must be equal to or after the `startDate`. Future dates (T+1) are not accepted in this endpoint.  Note: - If omitted (with `startDate` specified): Data will be returned from the specified start date up to the most recent available date. - If both `startDate` and `endDate` are omitted: The response will return the latest available data. 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/IndustryRevenueResponse} and HTTP response
     */
    getRbicsIndustryRevenueWithHttpInfo(rbicsIds, opts) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'rbicsIds' is set
      if (rbicsIds === undefined || rbicsIds === null) {
        throw new Error("Missing the required parameter 'rbicsIds' when calling getRbicsIndustryRevenue");
      }

      let pathParams = {
      };
      let queryParams = {
        'rbicsIds': this.apiClient.buildCollectionParam(rbicsIds, 'csv'),
        'startDate': opts['startDate'],
        'endDate': opts['endDate']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = [];
      let accepts = ['application/json'];


      let returnType = IndustryRevenueResponse;

      return this.apiClient.callApi(
        '/factset-rbics/v1/industry/revenue', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Get companies and their revenue exposure to a specific RBICS L6 Sub-Industry.
     * Screen for companies with revenue exposure to a specific RBICS Level 6 Sub-Industry. This endpoint identifies all companies that derive revenue from a specified sub-industry and returns the percentage of their total revenue attributable to that industry. This is a powerful tool for understanding the competitive landscape and supply chains at a granular level, beyond a company's primary classification. **Note:** The `rbicsIds` parameter for this endpoint must be a valid Level 6 Sub-Industry ID. Use the `startDate` and `endDate` parameters to control the time period for the underlying revenue reports. 
     * @param {Array.<String>} rbicsIds List of RBICS Level 6 Sub-Industry IDs to screen for companies by revenue exposure. **Note:** Only Level 6 IDs are supported for this endpoint. A complete list of valid Level 6 IDs can be retrieved using the `/structure` endpoints. 
     * @param {Object} opts Optional parameters
     * @param {Date} opts.startDate Specifies the start date for the requested date range, formatted as **YYYY-MM-DD**. The data returned will be reflective of the classifications or revenue data as of this date and forward. The `startDate` must be equal to or before the `endDate`. Future dates (T+1) are not accepted in this endpoint.  Note:  - If omitted while `endDate` is specified: Data will be fetched from the earliest available record up to the specified end date. - If both `startDate` and `endDate` are omitted: The response will return the latest available data. 
     * @param {Date} opts.endDate Specifies the end date for the requested date range, formatted as **YYYY-MM-DD**. The data returned will be reflective of the classifications or revenue data as of this date and earlier. The `endDate` must be equal to or after the `startDate`. Future dates (T+1) are not accepted in this endpoint.  Note: - If omitted (with `startDate` specified): Data will be returned from the specified start date up to the most recent available date. - If both `startDate` and `endDate` are omitted: The response will return the latest available data. 
     * @return { Promise.< module:model/IndustryRevenueResponse > } a Promise, with data of type {@link module:model/IndustryRevenueResponse }
     */
    getRbicsIndustryRevenue(rbicsIds, opts) {
      return this.getRbicsIndustryRevenueWithHttpInfo(rbicsIds, opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Get companies and their revenue exposure to a specific RBICS L6 Sub-Industry.
     * Screen for companies with revenue exposure to a specific RBICS Level 6 Sub-Industry. This endpoint identifies all companies that derive revenue from a specified sub-industry and returns the percentage of their total revenue attributable to that industry. This is a powerful tool for understanding the competitive landscape and supply chains at a granular level, beyond a company's primary classification. **Note:** The `rbicsIds` parameter for this endpoint must be a valid Level 6 Sub-Industry ID. Use the `startDate` and `endDate` parameters to control the time period for the underlying revenue reports. Use the POST method for large lists of `rbicsIds`. 
     * @param {module:model/IndustryRevenueRequest} industryRevenueRequest Request Body to screen for companies with revenue exposure to a specific RBICS L6 Sub-Industry.
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/IndustryRevenueResponse} and HTTP response
     */
    getRbicsIndustryRevenueForListWithHttpInfo(industryRevenueRequest) {
      let postBody = industryRevenueRequest;
      // verify the required parameter 'industryRevenueRequest' is set
      if (industryRevenueRequest === undefined || industryRevenueRequest === null) {
        throw new Error("Missing the required parameter 'industryRevenueRequest' when calling getRbicsIndustryRevenueForList");
      }

      let pathParams = {
      };
      let queryParams = {
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['FactSetApiKey', 'FactSetOAuth2'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];


      let returnType = IndustryRevenueResponse;

      return this.apiClient.callApi(
        '/factset-rbics/v1/industry/revenue', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Get companies and their revenue exposure to a specific RBICS L6 Sub-Industry.
     * Screen for companies with revenue exposure to a specific RBICS Level 6 Sub-Industry. This endpoint identifies all companies that derive revenue from a specified sub-industry and returns the percentage of their total revenue attributable to that industry. This is a powerful tool for understanding the competitive landscape and supply chains at a granular level, beyond a company's primary classification. **Note:** The `rbicsIds` parameter for this endpoint must be a valid Level 6 Sub-Industry ID. Use the `startDate` and `endDate` parameters to control the time period for the underlying revenue reports. Use the POST method for large lists of `rbicsIds`. 
     * @param {module:model/IndustryRevenueRequest} industryRevenueRequest Request Body to screen for companies with revenue exposure to a specific RBICS L6 Sub-Industry.
     * @return { Promise.< module:model/IndustryRevenueResponse > } a Promise, with data of type {@link module:model/IndustryRevenueResponse }
     */
    getRbicsIndustryRevenueForList(industryRevenueRequest) {
      return this.getRbicsIndustryRevenueForListWithHttpInfo(industryRevenueRequest)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


}





